module.exports = function (self) {

	self.log("debug", `---------------- variables def ` )

	const variables = []; 
/*
	variables.push({
		name: 'Status',
		variableId: 'statusId',
	});
	
	variables.push({
		name: 'Play Speed',
		variableId: 'speed',
	});
*/
	self.setVariableDefinitions(variables)
}
